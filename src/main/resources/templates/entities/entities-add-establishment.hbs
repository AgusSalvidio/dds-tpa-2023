<!DOCTYPE html>
<html lang="en">
<head>
    {{{headMeta}}}
</head>
<body>
<header>
    {{{customAdministrationNavbar}}}
</header>
<main>
    <section class="titles justify-content-center">
        <h2 class="title">{{title}} - ABM de Establecmientos</h2>
    </section>
    <section class="container">
        <form class="row g-3 py-2" method="post" action="{{action}}/{{registered_object.id}}/save-establishment">
            <div class="col-md-6">
                <label for="name" class="col-form-label">Entidad:</label>
                <input type="text" value="{{registered_object.name.name}}" class="form-control" name="name" disabled/>
            </div>
            <div class="col-md-6">
            </div>
            <div class="col-md-6">
                <label for="establishment" class="col-form-label">Establecimiento:</label>
                <select class="col-md-6 form-control" name="establishment">
                    <option label="Seleccione ..." value="" disabled selected></option>
                    {{#each establishments}}
                        <option value={{this.id}}>{{this.name}}</option>
                    {{/each}}
                </select>
            </div>
            <div class="d-flex justify-content-center gap-2 col-md-12">
                <a class="btn btn-secondary" href="{{action}}/{{registered_object.id}}/edit"><i class="fa fa-arrow-left"
                                                                                                aria-hidden="true"></i>Cancelar</a>
                <button type="submit" class="btn btn-primary"><i class="fa fab fad fas far fa-save"
                                                                 aria-hidden="true"></i>{{buttonActionLabel}}</button>
            </div>
        </form>
    </section>
</main>
<footer>
</footer>
{{{scripts}}}
</body>
</html>